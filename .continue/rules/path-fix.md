# Path Resolution Rule
We need to work around a tool bug. When using the 'ls' tool to list files in this project, never use '.' as the directory path. Always use the full absolute path of the workspace: /etc/nixos
